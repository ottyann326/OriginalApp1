<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= root_path %>" >ラケット一覧</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <% if user_signed_in? %>
          <div class="nav-link"></div> <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          <div class="nav-link"></div> <%= link_to "投稿画面", new_racket_path %>
          <div class="nav-link"></div> <%= link_to "マイページ", user_path(current_user.id) %>
        <% else %>
          <div class="nav-link"></div> <%= link_to "ログイン", new_user_session_path %>
          <div class="nav-link"></div> <%= link_to "投稿画面", new_racket_path %>
          <div class="nav-link"></div> <%= link_to "新規登録", new_user_registration_path %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
